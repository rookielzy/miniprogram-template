<block wx:for="{{ products }}" wx:for-item="product" wx:key="789">
  <view class="product-container" data-product_id="{{ product.id }}" bindtap="linkToProduct">
    <image src="{{ product.image_url }}" />
    <text class="product_name">{{ product.name }}</text>
    <text class="product_price">￥{{ product.price }}</text>
  </view> 
</block>
